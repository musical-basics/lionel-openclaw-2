# 2026-04-18

## [03:23] — Workspace structure reorganization
* Decision: Reorganized the workspace to follow Lionel's standard layout with bot system files at root, project repos under `projects/`, active specs in `plans/`, reference docs in `docs/`, data in `data/`, deploy configs in `deploy/`, and reports in `reports/`.
* Information: Moved the active Ultimate Pianist repo from `/home/openclaw/.openclaw/ultimate-pianist` to `/home/openclaw/.openclaw/workspace/projects/ultimate-pianist`.
* Information: Moved the Discord mentions reference note into `docs/discord-bot-mentions.md`, created `docs/workspace-structure.md`, created `reports/workspace-reorg-2026-04-18.md`, and added `data/state/.gitkeep` plus `deploy/.gitkeep` as scaffolding.
* Information: Updated `.gitignore` to preserve existing workspace-level ignores and ignore `projects/` so nested project repos stay isolated from the workspace repo.
* Task: Report the completed reorganization back to Commander and note that no separate copy of Lionel's structure guide was present in the local workspace, so the summarized directory rules in the request were used directly.

## [03:28] — Workspace reorg handoff
* Information: Reported the workspace reorganization back to Openclaw Commander in `#ultimate-pianist-channel`.
* Information: The handoff said the active Ultimate Pianist repo is now under `/home/openclaw/.openclaw/workspace/projects/ultimate-pianist` and the workspace `.gitignore` includes `projects/` to keep nested repos isolated.
* Task: If Lionel later shares the full written structure guide, compare the current layout against that source and adjust any mismatches.

## [03:29] — Structure guide received and aligned
* Information: Commander then shared the full `OpenClaw Bot Workspace Structure Guide` file in-channel as the source of truth for the workspace layout.
* Decision: Kept the new directory layout because it already matched the guide, then expanded `.gitignore` to the guide's minimum workspace ignore set.
* Information: The reorg blocker was cleared once the guide file arrived, so the workspace report now records no blockers.

## [03:32] — Workspace guide follow-up
* Correction: The actual workspace structure guide was later provided in-channel, so the earlier "guide not found locally" blocker was cleared.
* Information: Compared the workspace against the shared guide and aligned the remaining mismatch by expanding the workspace `.gitignore` to the guide's minimum ignore set plus `projects/`.
* Information: Updated the local workspace-structure note and reorg report to reflect the guide-based pass, with no blockers remaining.

## [03:33] — Hermes bot role split
* Decision: Lionel assigned Openclaw 2 to manage three Hermes bots for repo work.
* Information: The Hermes `planner` bot defines plans and build sequencing, the Hermes `developer` bot implements repo changes, and the Hermes `operator` bot runs operational actions such as SQL queries, tests, and function execution.
* Information: Openclaw 2 remains the manager and coordinator over that three-bot Hermes workflow.

## [03:36] — Hermes folder setup
* Information: Created separate Hermes bot areas under `/home/openclaw/.openclaw/workspace/projects/ultimate-pianist/hermes/` for `planner`, `developer`, and `operator`.
* Information: Each bot area now has its own `config/`, `workspace/`, and `state/` folders, plus a role-specific `config/bot.json` file.
* Decision: Used a manual in-repo scaffold instead of installing an unknown Hermes package, because no Hermes runtime or CLI was present or specified on the machine.
* Information: The remaining blocker to actually running all three bots is the missing Hermes runtime and launch method.
# 2026-04-18

## [03:23] — Workspace structure reorganization
* Decision: Reorganized the workspace to follow Lionel's standard layout with bot system files at root, project repos under `projects/`, active specs in `plans/`, reference docs in `docs/`, data in `data/`, deploy configs in `deploy/`, and reports in `reports/`.
* Information: Moved the active Ultimate Pianist repo from `/home/openclaw/.openclaw/ultimate-pianist` to `/home/openclaw/.openclaw/workspace/projects/ultimate-pianist`.
* Information: Moved the Discord mentions reference note into `docs/discord-bot-mentions.md`, created `docs/workspace-structure.md`, created `reports/workspace-reorg-2026-04-18.md`, and added `data/state/.gitkeep` plus `deploy/.gitkeep` as scaffolding.
* Information: Updated `.gitignore` to preserve existing workspace-level ignores and ignore `projects/` so nested project repos stay isolated from the workspace repo.
* Task: Report the completed reorganization back to Commander and note that no separate copy of Lionel's structure guide was present in the local workspace, so the summarized directory rules in the request were used directly.

## [03:28] — Workspace reorg handoff
* Information: Reported the workspace reorganization back to Openclaw Commander in `#ultimate-pianist-channel`.
* Information: The handoff said the active Ultimate Pianist repo is now under `/home/openclaw/.openclaw/workspace/projects/ultimate-pianist` and the workspace `.gitignore` includes `projects/` to keep nested repos isolated.
* Task: If Lionel later shares the full written structure guide, compare the current layout against that source and adjust any mismatches.

## [03:29] — Structure guide received and aligned
* Information: Commander then shared the full `OpenClaw Bot Workspace Structure Guide` file in-channel as the source of truth for the workspace layout.
* Decision: Kept the new directory layout because it already matched the guide, then expanded `.gitignore` to the guide's minimum workspace ignore set.
* Information: The reorg blocker was cleared once the guide file arrived, so the workspace report now records no blockers.

## [03:32] — Workspace guide follow-up
* Correction: The actual workspace structure guide was later provided in-channel, so the earlier "guide not found locally" blocker was cleared.
* Information: Compared the workspace against the shared guide and aligned the remaining mismatch by expanding the workspace `.gitignore` to the guide's minimum ignore set plus `projects/`.
* Information: Updated the local workspace-structure note and reorg report to reflect the guide-based pass, with no blockers remaining.

## [03:33] — Hermes bot role split
* Decision: Lionel assigned Openclaw 2 to manage three Hermes bots for repo work.
* Information: The Hermes `planner` bot defines plans and build sequencing, the Hermes `developer` bot implements repo changes, and the Hermes `operator` bot runs operational actions such as SQL queries, tests, and function execution.
* Information: Openclaw 2 remains the manager and coordinator over that three-bot Hermes workflow.

## [03:36] — Hermes folder setup
* Information: Created separate Hermes bot areas under `/home/openclaw/.openclaw/workspace/projects/ultimate-pianist/hermes/` for `planner`, `developer`, and `operator`.
* Information: Each bot area now has its own `config/`, `workspace/`, and `state/` folders, plus a role-specific `config/bot.json` file.
* Decision: Used a manual in-repo scaffold instead of installing an unknown Hermes package, because no Hermes runtime or CLI was present or specified on the machine.
* Information: The remaining blocker to actually running all three bots is the missing Hermes runtime and launch method.

## [03:49] — Hermes availability check
* Correction: A Hermes base CLI does exist on the local VPS at `/home/openclaw/.local/bin/hermes`, so the earlier assumption that no Hermes CLI was present was incorrect.
* Information: Verified agent availability with `/home/openclaw/.local/bin/hermes profile show planner`, `/home/openclaw/.local/bin/hermes profile show developer`, and `/home/openclaw/.local/bin/hermes profile show operator`.
* Information: At check time, `planner`, `developer`, and `operator` were not available as registered Hermes profiles on the VPS.
* Information: The current blocker is no longer the base Hermes runtime, but that those three named Hermes profiles have not been created or registered yet.

## [04:13] — Hermes planner becomes librarian
* Decision: Lionel changed the Hermes role plan so the `planner` bot is now also the Ultimate Pianist librarian.
* Information: The `planner` bot now owns file-memory and indexing responsibilities for Ultimate Pianist files, structure, locations, and organizational drift, not just sequencing and spec planning.
* Information: This planner-plus-librarian role is the current source of truth unless Lionel changes it again.

## [05:01] — Hermes planner identity mapping
* Decision: Lionel clarified that `Hermes UltPianist 1` is the active Hermes `planner` and `librarian` working under Openclaw 2.
* Information: This means the planner-plus-librarian role now maps to a concrete Hermes bot identity, not just an abstract role name.
* Information: Openclaw 2 should treat `Hermes UltPianist 1` as the planner/librarian subordinate for Ultimate Pianist unless Lionel changes that assignment.

## [05:01] — Planner/librarian source-of-truth confirmation
* Information: Openclaw Commander restated that `Hermes UltPianist 1` is the Ultimate Pianist planner/librarian under Openclaw 2 coordination.
* Decision: Treat that assignment as the active source of truth in repo and workspace context unless Lionel changes it.

## [05:04] — Librarian role file created
* Decision: Added a dedicated markdown role brief for the Ultimate Pianist planner/librarian at `projects/ultimate-pianist/hermes/planner/LIBRARIAN.md`.
* Information: The file defines the librarian mission, scope, responsibilities, working rules, outputs, and coordination under Openclaw 2.
* Information: Updated the planner bot config and repo readmes to point to that role file as the current written brief for Hermes UltPianist 1.

## [05:06] — Librarian brief tightened
* Information: Refined `projects/ultimate-pianist/hermes/planner/LIBRARIAN.md` so it explicitly says the librarian tracks file locations and source-of-truth docs, maintains structure awareness, flags duplicates/stale files/drift, helps planner/developer/operator find the right file before editing, and records structural changes plus repo-map updates.

## [05:08] — Manager and librarian role check
* Decision: Reconfirmed that Openclaw 2 is the Ultimate Pianist manager/coordinator who supervises workflow and routes work across planner, developer, and operator.
* Information: Reconfirmed that `Hermes UltPianist 1` is the planner/librarian under Openclaw 2, owning planning plus file-memory and structure stewardship.
* Information: Updated repo context notes so the manager role and librarian coordination are explicit written source of truth unless Lionel changes the split.

## [05:11] — Ultimate Pianist team hierarchy clarified
* Decision: Lionel clarified the current hierarchy as Openclaw Commander = head of the company, Openclaw 2 = Ultimate Pianist team manager, and `Hermes UltPianist 1` = Ultimate Pianist team librarian.
* Information: I treated this as a hierarchy clarification layered on top of the existing manager and librarian role split, and updated repo context notes accordingly.

## [05:11] — Librarian-first role refinement
* Decision: Openclaw Commander refined the Ultimate Pianist role label so `Hermes UltPianist 1` is librarian first under Openclaw 2, while still retaining planning support when useful.
* Information: I updated repo docs and planner config to reflect librarian-first wording without renaming the existing `hermes/planner/` folder slot.

## [05:17] — Hermes live profile path correction
* Correction: The real Hermes runtime/profile directories are under `/home/openclaw/.hermes/profiles/`, not under `projects/ultimate-pianist/hermes/` in the repo.
* Information: Verified live paths at `/home/openclaw/.hermes/profiles/planner/config.yaml`, `/home/openclaw/.hermes/profiles/planner/SOUL.md`, `/home/openclaw/.hermes/profiles/developer/config.yaml`, and `/home/openclaw/.hermes/profiles/operator/config.yaml`.
* Information: Verified with `/home/openclaw/.local/bin/hermes profile show planner`, `developer`, and `operator`; all three profiles now exist, with planner running and developer/operator stopped at check time.
* Decision: Updated project repo docs so `projects/ultimate-pianist/hermes/` is described as project-side role/scaffold documentation and scratch space, not the live Hermes config location.

## [05:32] — Ultimate Pianist developer identity recorded
* Decision: Openclaw Commander set `Hermes UltPianist 2` as the Ultimate Pianist developer under Openclaw 2.
* Information: Exact Discord user id for `Hermes UltPianist 2` is `1494930467621830787`.
* Information: Exact Discord mention for `Hermes UltPianist 2` is `<@1494930467621830787>`.
* Information: Updated project-side Hermes developer notes so the identity mapping is recorded in repo context.

## [05:37] — Developer mapping normalized across role maps
* Decision: Treated the active Ultimate Pianist team-role map as Openclaw Commander = head-level coordinator, Openclaw 2 = manager, `Hermes UltPianist 1` = librarian, and `Hermes UltPianist 2` = developer under Openclaw 2.
* Information: Normalized the `Hermes UltPianist 2` mapping across project role-map files with Discord user id `1494930467621830787`, display label `@Hermes UltPianist 2`, and actual ping mention `<@1494930467621830787>`.
* Information: Updated the project root README, Hermes README, planner librarian brief, and developer bot config so the same developer identity mapping appears wherever the team-role map is documented.

## [05:45] — Hermes delegation rule corrected
* Correction: Lionel said not to @mention Hermes bots from Discord bot sessions because Hermes bots do not hear bots there; they only respond to Lionel in Discord.
* Decision: Delegate Hermes work through CLI instead of Discord mentions, using one specific task per call such as `planner chat -q "do X"` or `developer chat -q "do Y"`.
* Information: Verified the base Hermes CLI path remains `/home/openclaw/.local/bin/hermes` on this host.
* Information: In non-interactive exec shells, the short `planner`, `developer`, and `operator` wrapper commands are not currently on `PATH`.

## [05:51] — Exact Hermes CLI delegation patterns verified
* Information: Verified Hermes CLI help locally and confirmed `chat -q` is the single-query pattern.
* Information: Verified planner profile alias path is `/home/openclaw/.local/bin/planner`.
* Information: Verified developer profile alias path is `/home/openclaw/.local/bin/developer`.
* Decision: For non-interactive delegation from Openclaw 2, use absolute alias paths with one specific task per call.
